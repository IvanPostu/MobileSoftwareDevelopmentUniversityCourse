import React, { ReactElement, FC } from 'react'
import ApplicationRouterWrapper from '@/routes/AppRouter'

const App: FC = (): ReactElement => {
  return <ApplicationRouterWrapper />
}

export default App
